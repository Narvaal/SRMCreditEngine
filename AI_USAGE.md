# AI_USAGE.md

Registro do uso de IA (Claude Code) no desenvolvimento do SRM Credit Engine, conforme a política de uso de IA do desafio (ver `CLAUDE.md`, seção 2). Este documento é atualizado incrementalmente ao longo do desenvolvimento, não apenas ao final.

## Ferramenta utilizada

- **Claude Code** (Anthropic) — sessões via CLI. Modelo Sonnet 5 nas sessões iniciais (2026-07-03/04) e Fable 5 a partir de 2026-07-05 (co-autoria registrada nos trailers dos commits).

## Prompts estratégicos utilizados

| Data | Prompt (resumo) | Objetivo | Resultado |
|------|------------------|----------|-----------|
| 2026-07-03 | "escreva um claude md com essas informações [enunciado completo do desafio]" | Registrar o enunciado do desafio como contexto persistente do projeto, para orientar sessões futuras de desenvolvimento assistido por IA | `CLAUDE.md` criado com o enunciado completo estruturado por seção, mais notas práticas de orientação (stack, precisão decimal, workflow de Git por senioridade) |
| 2026-07-03 | "crie tmb o AI_USAGE.md" | Criar o arquivo de rastreabilidade de uso de IA exigido pelo desafio | Este arquivo |
| 2026-07-03 | Sequência de perguntas pedindo pra explicar o problema de negócio "da forma mais simples possível" (fluxo de deságio, ordem da conversão de moeda, o que é um lote de recebíveis) | Construir entendimento do domínio antes de tomar qualquer decisão técnica (metodologia SDD) | Explicações em linguagem simples com exemplos numéricos concretos, sem gerar código — usadas como base para as decisões registradas em seguida |
| 2026-07-03 | Fechamento guiado das perguntas em aberto do `ROADMAP.md` (granularidade da liquidação por lote vs. por item; origem da Taxa Base — mercado vs. interna; convenção de prazo em dias vs. meses; existência de estorno/cancelamento; controle de saldo de caixa por moeda) | Eliminar as ambiguidades de negócio do enunciado antes de especificar modelo de dados e API | 5 decisões de domínio registradas no `ROADMAP.md`, cada uma com a justificativa de negócio por trás: (1) liquidação independente por recebível, não por lote; (2) Taxa Base é taxa de mercado (CDI/SOFR) com histórico, spread é política interna da SRM; (3) prazo em meses, mês fixo = 30 dias, convenção simplificada e assumida conscientemente; (4) histórico append-only, correções via estorno, nunca reescrita do passado; (5) caixa com saldo controlado por moeda, habilitando o cenário real de Optimistic Locking do nível Sênior |
| 2026-07-03 | "faça um plano e me apresente" (modelo de dados) — rascunho inicial de 8 entidades revisado por um agente de planejamento dedicado, com instrução explícita para criticar tipos numéricos, índices, constraints e apontar entidades/relacionamentos faltantes sob a ótica de um sistema financeiro real | Validar o esquema de dados antes de implementar as migrations, evitando erros de precisão numérica ou modelagem que só apareceriam depois de código de aplicação já escrito em cima | O agente encontrou problemas reais no rascunho: `NUMERIC` sem precisão/escala definida (risco real de inconsistência num sistema financeiro), `recebivel.status` binário incompleto (não suportava o fluxo de estorno já decidido), e CHECK constraints de integridade ausentes (ex.: invariante moeda-título × taxa-de-câmbio). Também sugeriu 3 extensões de escopo (tabela `lote_importacao`, ponteiro redundante em `recebivel`, spread no banco vs. no código) — cada uma foi levada de volta pro usuário decidir via pergunta objetiva, não aceita automaticamente. Onde a IA ajudou: achou a lacuna de `NUMERIC` genérico e o `status` binário incompleto, que eu não tinha notado no rascunho. Onde exigiu filtro humano: sugeriu adicionar `movimento_caixa` e `liquidacao.criado_por` "de graça" — decidi adiar as duas por escopo (a segunda nem faz sentido ainda, não existe sistema de usuário) em vez de aceitar por padrão só porque a IA recomendou. |
| 2026-07-03 | "o docker compose já tá pronto, se não tiver faça ele subir o app e banco de dados" | Orquestrar API + PostgreSQL + Prometheus + Grafana num único `docker compose up`, item já pendente no checklist do README | `docker-compose.yml` + `backend/Dockerfile` (build multi-stage) + `infra/prometheus/prometheus.yml` + provisionamento automático do datasource do Grafana. Validado de verdade: build da imagem, subida dos 4 containers, migrations aplicando no boot, `/actuator/health` respondendo, Prometheus com o target `up`, Grafana com o datasource já configurado — e um ciclo completo `down -v` + `up -d` do zero pra garantir que não era só cache local funcionando por acaso |
| 2026-07-03 | Discussão de arquitetura (Controller/Service/DAO, depois refinada pra Controller/Service/Repository + caminho de 2 camadas pro relatório) seguida de "faça um plano e me apresente" — plano revisado por um agente dedicado com perguntas técnicas específicas (potência fracionária em `BigDecimal`, geração de UUID em JPA, mecânica de Optimistic Locking, assinatura do Strategy) | Implementar a camada de aplicação inteira (entidades JPA, motor de precificação, services transacionais, controllers, relatório) em cima do modelo de dados já validado | Aplicação funcional de ponta a ponta: 9 entidades, motor de precificação com `big-math` pra expoente fracionário, `LiquidacaoService` com Optimistic Locking real (flush explícito + tradução de exceção), 15 testes unitários de precificação, endpoints REST completos, relatório em 2 camadas com SQL nativo. Validado com `docker compose` do zero + smoke test manual completo + teste de concorrência real via 2 requisições HTTP simultâneas |
| 2026-07-03 | "faça um plano para implementar o front, gostaria algo minimalista e moderno, com as cores verde e preto" — plano revisado por um agente dedicado, que primeiro apontou que o requisito de "cálculo em tempo real" não era implementável sem um endpoint novo de simulação (duplicar a fórmula no frontend duplicaria regra de negócio) | Implementar o Painel do Operador e o Grid de Transações com a arquitetura de separação apresentação/negócio exigida pelo desafio | 3 endpoints novos no backend (`/simular`, `/moedas`, `/tipos-recebivel`) + frontend completo (Tailwind v4 tema escuro/verde, React Router, TanStack Query, React Hook Form + Zod), validado com Playwright headless (simulação em tempo real, submissão de lote, navegação, zero erros de console) |
| 2026-07-03 | "faça o workflow de ci/cd usando o github actions e adicione o frontend ao docker-compose" | Fechar o requisito de CI/CD do nível Sênior e eliminar a lacuna do frontend não containerizado | `frontend/Dockerfile` (multi-stage, Nginx com reverse proxy de `/api` pro backend) + `docker-compose.yml` com o serviço `frontend` + `.github/workflows/ci.yml` (3 jobs: backend, frontend, smoke test do compose completo). Validado com `docker compose up -d --build` real (5 containers, proxy funcionando, fallback de SPA) e o YAML do workflow checado com `actionlint` (baixado no scratchpad, já que `act` não estava disponível pra rodar Actions localmente) |
| 2026-07-04 | "leia o roadmap e readme... quero que vc me apresente um relatorio do que devemos testar" seguido de "vamos fazer na ordem" (execução item a item) | Fechar a lacuna de cobertura de testes identificada no próprio `ROADMAP.md` (`LiquidacaoService` só testado indiretamente; vários services sem teste próprio) | Relatório priorizado (P0/P1/P2) mapeando cada gap contra o código real, seguido de implementação incremental: `LiquidacaoServiceTest` (14 casos: liquidar/estornar, saldo insuficiente, Optimistic Locking, estorno duplicado), `LiquidacaoBatchServiceTest` (resultado parcial do lote), `CambioServiceTest`, `TaxaMercadoServiceTest`, `RecebivelServiceTest`, `CedenteServiceTest`, `GlobalExceptionHandlerTest` (68 testes de unidade no total, backend 100% verde exceto a limitação conhecida do Testcontainers), e no frontend `usePainelOperadorForm.test.tsx` + `useExtratoFiltrosUrlState.test.tsx` |
| 2026-07-04 | "faca os comits atomicos, e faca rebase para contar a historia de melhor forma" | Praticar o requisito de Git explícito do nível Sênior (organizar/reordenar commits via `git rebase -i` antes de um merge), até então só planejado e nunca demonstrado | 7 commits atômicos (`test(backend): ...`, um por classe testada) criados fora da ordem narrativa ideal de propósito, depois reordenados via `git rebase -i` (com `GIT_SEQUENCE_EDITOR` roteirizado, sem editor interativo) pra sequência núcleo-transacional → services de domínio → cross-cutting handler — suíte revalidada verde após o rebase antes do push |
| 2026-07-04 | "vamos atacar o problema dos logs estruturados, faca um plano para implementar, entenda qual a melhor opcao para o projeto ecs ou gelf e me apresente" | Fechar outro requisito explícito do nível Sênior (observabilidade via logs estruturados), decidindo entre os dois formatos nativos do Spring Boot 3.5 | Levantamento do estado real (zero config de logging, só 2 linhas de log em todo o backend) fundamentou a recomendação por **ECS** em vez de GELF (GELF pressupõe um Graylog que o projeto não roda; ECS é o formato nativo tanto de um eventual Elastic quanto do Grafana Loki, extensão natural do Grafana já provisionado). Plano aprovado via `EnterPlanMode`/`ExitPlanMode` antes de codar. Implementado: `logging.structured.format.console: ecs`, `CorrelationIdFilter` (MDC `requestId` por requisição) e logs de negócio estruturados (`addKeyValue`) em `LiquidacaoService`/`LiquidacaoBatchService`/`GlobalExceptionHandler` |
| 2026-07-04 | "quero que voce cubra os testes que ainda estao em aberto" (backend P2 + frontend P1 do relatório de cobertura) | Fechar os últimos gaps de teste antes do merge/release: controllers, relatório (SQL nativo) e componentes de composição do frontend | `@WebMvcTest` nos 8 controllers, `ExtratoLiquidacaoRepositoryIT` (Testcontainers), e no frontend `useExtratoLiquidacaoQuery`/`RecebivelForm`/`FiltrosTransacoes`/`PainelOperadorPage`/`GridTransacoesPage`. Escrever os testes achou 2 bugs reais de verdade (detalhados na seção abaixo), corrigidos na hora: `@RequestParam` obrigatório ausente devolvendo 500 em vez de 400, e campos de filtro sem `name`/`id` quebrando a associação `label`↔`input` (acessibilidade) |
| 2026-07-04 | "escreva um documento com os Critérios de aceite documentados... e tambem o diagrama c4, nivel 1 e 2" | Fechar os dois últimos requisitos de documentação explícitos do desafio (seções 5 e 6) antes do merge/release | `docs/diagrama-c4.md` (Mermaid `C4Context`/`C4Container`, mesma convenção do diagrama ER) e `docs/criterios-aceite.md` (tabela verificável por dimensão — usabilidade/segurança/desempenho/escalabilidade — com status e evidência por item, não prosa vaga). Levantamento do código real (ausência de Spring Security, Dockerfiles sem `USER`, `docker-compose` em HTTP puro, sem cache/teste de carga) tornou explícitos gaps que já existiam implicitamente mas nunca tinham sido escritos num lugar só |
| 2026-07-04 | "ele ta meio estranho visualmente... tem alguma forma de alterar isso" (diagrama C4) | Corrigir texto sobreposto e caixas pequenas nos diagramas C4 renderizados | Diagnosticado como limitação do motor de layout do tipo `C4Context`/`C4Container` nativo do Mermaid (caixas de tamanho fixo). Reescrito como `flowchart` estilizado como C4 (classDefs pra `person`/`system`/`container`/`external`), com o motor de layout padrão do Mermaid (dagre), que ajusta caixa ao conteúdo. Validado renderizando de verdade pra PNG via `mermaid-cli` (não só lendo o código-fonte) antes de cada commit, inclusive iterando sobre uma aresta que visualmente parecia sair do PostgreSQL em vez do backend (artefato de layout, corrigido com estilo tracejado + nota explicativa) |
| 2026-07-04 | "vamos fazer o merge dev → main" seguido de "cria a tag v1.0.0" e "faz os dois: atualiza o roadmap e cria a release" | Primeira promoção do projeto completo pra `main` e marco de versão semântica, fechando os últimos critérios de Git do nível Sênior (`CLAUDE.md`, seção 6) | PR #1 (`dev → main`, 71 commits, CI validado no PR antes do merge) com descrição resumindo os 10 passos do roadmap; merge commit (não squash, preserva a granularidade dos commits atômicos); tag anotada `v1.0.0` sobre o commit de merge; GitHub Release publicada a partir da tag. Confirmado com `gh run watch` que o CI também passa direto em `main` depois do merge, não só no PR |
| 2026-07-04 | "qual vai ser a utilidade da branch prod" seguido de "faca a opcao b" (usar `prod` só pra demonstrar o exercício de gestão de crise do nível Especialista, não fingir um ambiente real) | Justificar a estratégia de branching (pendência antiga do `ROADMAP.md`) e demonstrar `git cherry-pick` de hotfix — requisito explícito de `CLAUDE.md`, seção 6 | Fix real usado como veículo (backend não-root, gap `S9` já documentado) — não um bug fabricado: implementado e validado em `dev` (container rodando contra Postgres real, `id` confirmando `uid=999`), PR #2 `dev → main` normal, depois `git cherry-pick` do commit específico do fix (não o merge commit, não o resto de `main`) direto pra `prod`, tag `v1.0.1` + GitHub Release. Documentado no README (nova seção "Estratégia de branching") e no `ROADMAP.md` (Passo 12) como o exercício de processo que é, não uma alegação de infraestrutura de produção que não existe |
| 2026-07-04 | "faca um plano para implementarmos mais tarde [Resiliência]" seguido de "pode executar o plano" — plano aprovado via `EnterPlanMode`/`ExitPlanMode` com 2 decisões fechadas por pergunta objetiva (mock interno vs. WireMock; trigger manual+scheduled vs. só manual) | Fechar o último requisito explícito do nível Sênior (retry/circuit breaker), que estava sem "alvo" — não existia nenhuma chamada HTTP externa no sistema | Provider externo simulado com HTTP de verdade (`/mock/fx-provider`, com knob de falha e contador de chamadas pra asserção determinística), `FxProviderClient` com Resilience4j (`@Retry` + `@CircuitBreaker`; tradução de falha no service via try/catch, mantendo o padrão do projeto em vez de `fallbackMethod` via AOP), e fallback com semântica de negócio real: provider fora degrada só a atualização de taxas, liquidação segue com a última taxa persistida. `FxProviderResilienceIT` verde no CI na primeira execução; ciclo completo closed→open→half-open→closed observado manualmente nas métricas do Prometheus. Detalhe pego na verificação manual (não no código): com o scheduler ligado no compose, a janela do circuito já tinha sucessos anteriores e o circuito abriu no meio do retry (2 chamadas, não 3) — comportamento correto do `minimum-number-of-calls`, e a matemática do IT se mantém determinística porque lá o scheduler fica desligado |
| 2026-07-04 | "depois disso verifique o projeto inteiro e encontre possiveis pontos de atencao que fogem do que eh pedido do senior" (auditoria final, Parte 2 do plano) | Varredura sistemática do projeto contra o `CLAUDE.md` em 5 dimensões, com relatório P0/P1/P2 e fixes só mediante aprovação | Conformidade Júnior→Pleno→Sênior 100% com evidência. 5 achados reais, todos aprovados e corrigidos: **fundamentação teórica da stack** prometida no próprio roadmap e nunca escrita (P0 — critério de avaliação 8.1 descumprido); **pre-push hook permanentemente bypassado** porque os `*IT` sempre falham localmente, o que pulava também os testes de frontend (P1 — hook agora roda só `--tests '*Test'`); roadmap prometendo branches `feature/*` nunca usadas vs. fluxo real trunk-based (P1 — nota de honestidade); contagens de teste desatualizadas e badge de release preso na v1.0.0 (P1); escopo Especialista implícito (P2 — nota explícita no README). A auditoria olhou inclusive pra promessas feitas pela própria dupla usuário+IA em sessões anteriores — o gap P0 era literalmente uma frase nossa de 2 dias antes que ninguém tinha cumprido |
| 2026-07-04 | "vamos implementar o nice to have tambem faca um plano" (UI de estorno, cadastro de cedente inline, frontend não-root) — plano aprovado via `EnterPlanMode`/`ExitPlanMode` | Fechar 3 dos 4 nice-to-haves restantes do `ROADMAP.md` | Backend ganhou a flag `estornada` no extrato (subquery `EXISTS`) pra UI só oferecer estorno onde ele pode suceder; Grid com coluna "Ações" e confirmação inline em dois cliques; `CadastroCedenteInline` com auto-seleção; frontend na imagem `nginx-unprivileged` (uid 101). **Bug real pego por teste de composição, não por leitura de código**: o `setValue` do cedente recém-criado rodava antes de a `<option>` existir no DOM (refetch do catálogo ainda não renderizado), deixando o select vazio — reproduziria igual no navegador; corrigido com efeito condicionado à lista re-buscada. Validação final com Playwright contra o `docker compose` real (cadastro inline → liquidação → estorno via UI, zero erros de console), incluindo a constatação de que as taxas da simulação vieram sozinhas do provider mockado via scheduler — a stack se auto-alimenta desde o Passo 13 |
| 2026-07-05 | "no front tem algumas coisas que precisamos alterar... cadastrar novo cedente nao tem nenhuma validacao [de] cpf... valor da face... coloque R$... vencimento tmb nao tem validacao... recebo a menssagem Payload inválido... faca um plano" — achados do próprio usuário testando a release v1.2.0 no Docker; plano aprovado via `EnterPlanMode`/`ExitPlanMode`, com escopo backend (validar CPF/CNPJ também na API) decidido por pergunta objetiva, e a validação da razão social adicionada pelo usuário na revisão do plano | Fechar lacunas de validação de input e clareza do Painel do Operador ("uma boa interface é uma que o usuário entende como usar") | Frontend: `lib/documento.ts` (máscara progressiva CPF→CNPJ + dígito verificador módulo 11, zero dependência nova) com erros por campo no `CadastroCedenteInline`, razão social 3–20 caracteres sem especiais, prefixo R\$/US\$ no valor de face (símbolo via `Intl.formatToParts`, acompanha a moeda do título), vencimento futuro validado no Zod (espelha o `@Future` do backend — o operador não vê mais "Payload inválido"), tipo de recebível sem placeholder com Duplicata Mercantil default, e `ApiError.camposInvalidos` (antes ignorado) mapeado pro campo correspondente. Backend: constraint `@DocumentoValido` + regras de nome no `CedenteRequest` (defesa em profundidade — API rejeita documento inválido mesmo via curl). 4 commits atômicos, testes novos em cada camada |
| 2026-07-05 | "encontrei outros problemas... posso color digitos demais no valor face... com tudo isso ele ainda calcula, limite o inpute para no maximo quadrilhoes... a data pode ser 01/05/7000... limite para no maximo 100 anos" — segunda rodada de teste exploratório do usuário na UI | Fechar limites de input do valor de face e do vencimento, e polir o campo de documento (label "Documento CPF/CNPJ", sem placeholder) | Teto de 1 quadrilhão no valor de face — escolhido cruzando o pedido do usuário com dois limites reais: o `NUMERIC(18,2)` do banco (16 dígitos inteiros) e a precisão inteira exata do IEEE-754 (2^53 ≈ 9 quadrilhões); vencimento limitado a hoje+100 anos. Validação no Zod + atributos `min`/`max` nos inputs + espelho no backend (`@DecimalMax`/`@Digits` nos DTOs de recebível e simulação, protegendo contra overflow do NUMERIC via API direta). **Bug pego só dirigindo o navegador real (Playwright), invisível nos 95 testes de unidade**: a validação nativa do HTML5 (atributo `max`) interceptava o submit e mostrava um tooltip em inglês estilizado pelo browser antes de o Zod rodar — as mensagens inline em português nunca apareciam; corrigido com `noValidate` no form (atributos continuam guiando date picker/spinner, mas as mensagens são sempre as do app). De quebra, a verificação achou que a mensagem default do `@Digits` vinha em inglês pro cliente ("numeric value out of bounds") — localizada antes do push |
| 2026-07-05 | "500000,032 um digito a mais depois da virgula eu nao tenho nenhuma menssagem... coloque uma menssagem no painel de resultado para avisar que tem um erro... deixe todas as menssagens no imperativo seguindo esse padrao - Informe um CPF válido." — terceira rodada de teste do usuário, incluindo a definição do padrão de tom das mensagens | Fechar os últimos atritos de feedback do Painel: casas decimais sem mensagem, painel de resultado cego a erros e mensagens sem padrão | Validação de 2 casas decimais no Zod; o hook do painel passou a distinguir formulário **incompleto** (campo vazio → "Preencha os dados...") de formulário **com erro** (campo preenchido que falhou → "Corrija os dados destacados..."), decisão de UX derivada diretamente da reclamação; validação em modo `onTouched` pro destaque aparecer sem submit; catálogo completo de mensagens (frontend e backend) reescrito no imperativo definido pelo usuário. Verificação de novo com Playwright no compose real: erro aparece ao sair do campo, painel avisa, e corrigir o valor faz a simulação voltar sozinha |
| 2026-07-05 | "500,0000000000000012355464564 passa... faca a messaagem so aparecer quando o erro aparecer... deixe a messagem da mesma cor" — o usuário pegou um bug real na validação de decimais da rodada anterior | Corrigir a validação de casas decimais e sincronizar timing/cor do aviso do painel | **Bug de ponto flutuante achado pelo usuário, não pelos testes**: a validação de decimais rodava depois do `z.coerce.number`, e `500,0000000000000012...` colapsa pra exatamente `500` no IEEE-754 — a regra via um número já "limpo". Corrigido validando a **string digitada** antes da conversão (union string/number → regex → transform → number), com teste que documenta o colapso. O aviso do painel agora só aparece quando o erro do campo está visível (gate por `formState.errors`, casando com o `onTouched`) e na cor neutra do texto padrão — o destaque visual fica no erro do campo, decisão de hierarquia visual do próprio usuário |
| 2026-07-05 | "consegui colocar o valor 0,010000000 aparentemente as casas sao ignoradas no front, mas nao sei se esse eh o certo que vc acha" — o usuário pediu diagnóstico, não só fix | Investigar o comportamento antes de opinar: reprodução com Playwright digitando tecla a tecla em dois locales (en-US e pt-BR) | Diagnóstico pior que o sintoma: o `<input type="number">` nativo **descarta a vírgula digitada em silêncio** — "0,010000000" virava "0010000000" no DOM, ou seja, R\$ 0,01 digitado virava R\$ 10.000.000 sem nenhum erro (corrupção silenciosa de input num sistema financeiro). As "casas ignoradas" que o usuário viu eram os dígitos re-concatenados sem a vírgula. Corrigido trocando pra `type="text"` + `inputMode="decimal"` (teclado numérico no mobile), com a string crua validada pelo Zod (formato numérico → casas decimais → conversão), mensagem própria pra texto não numérico, e verificação tecla a tecla no navegador confirmando: vírgula preservada, erro no blur, e "0,01" simulando exatamente R\$ 0,01 |
| 2026-07-05 | "pra finalizer coloque os . automaticamente no valor digitado" | Fechar a rodada de UX do valor de face com máscara de milhar pt-BR | `mascararValorBR` em `lib/formatters.ts` (pontos de milhar em tempo real, vírgula decimal, ponto digitado em posição decimal vira vírgula — proteção contra o teclado numérico); schema do valor passa a interpretar formato brasileiro (ponto = milhar) e converte com segurança; máscara deliberadamente **não** trunca casas decimais em excesso (vira erro visível do Zod, coerente com a decisão da rodada anterior de nunca corrigir input em silêncio). Verificado tecla a tecla no navegador: "1234567,89" → "1.234.567,89" simulando R\$ 1.061.832,45 de líquido |
| 2026-07-05 | "quando eu clico no botao estornar quero que abra um modal com todas as informacoes da transacao... botao de confirmar verde e um de cancelar transparente... coloque tambem as menssagens tipo estornado com sucesso ou o erro no modal" | Substituir a confirmação inline de estorno (dois cliques) por um modal completo na Grid de Transações | Componente `Modal` novo no design system (overlay, Esc, clique-fora, `role="dialog"`) + `EstornoModal` na feature com os dados completos da transação (data, cedente, tipo, moedas, valores, deságio com a mesma regra cross-currency da tabela, id), confirmar `primary` verde e cancelar `ghost` transparente; sucesso/erro da mutação aparecem dentro do modal (o `Alert` de página saiu). Testes de componente pros dois novos + fluxo completo na página; verificação Playwright contra o compose real: abrir → cancelar → confirmar → "Liquidação estornada com sucesso." → grid atualizada com a linha ESTORNO |
| 2026-07-05 | "se o usuario clicar em confirmar feche o modal e exiba um toast... lembre de travar os outros botoes para que nao aconteca 2 transacoes enquanto uma esta rolando" (+ ajustes de botão centralizado e padding da tabela) | Refinar o fluxo de estorno: modal só confirma, resultado assíncrono vai pra toast, e a UI impede estornos concorrentes | Componente `Toast` novo no design system (auto-dismiss 5s, `role="status"`/`aria-live`, botão de fechar); confirmar fecha o modal imediatamente e o sucesso/erro da API chega no toast; todos os botões Estornar da grid ficam `disabled` enquanto a mutação está pendente (trava de concorrência na UI, complementando o Optimistic Locking que já existe no backend). De quebra, o ajuste do padding revelou um bug real no design system: o `Th` sobrescrevia o className base em vez de mesclar (como o `Td` faz) — todo cabeçalho `text-right` renderizava sem padding. Verificação Playwright com resposta da API atrasada em 1,5s pra observar o estado intermediário: modal fecha na hora, botões travados durante o voo, toast aparece, botões destravam, toast some sozinho |
| 2026-07-05 | Spec estruturada do usuário (objetivo, novo comportamento, layout, UX): fundir LIQUIDACAO+ESTORNO numa linha só de estado final, expansível pra revelar a "Operação original", sem tocar no backend | Melhorar a leitura do Grid: consulta rápida na tabela, detalhe da origem do estorno sem navegar pra outra tela | Transformação pura em `domain/extratoAgrupado.ts`: como o extrato não traz o vínculo estorno→liquidação, o pareamento usa o recebível (estorno desfaz a liquidação estornada mais recente anterior a ele, com consumo pra suportar recebível re-liquidado); pares que caem em páginas diferentes degradam pro comportamento antigo — nunca se esconde uma operação sem conseguir mostrar o estado final. Linha exibida = data/tipo do estorno + dados da liquidação; seta expande a "Operação original" logo abaixo (fundo diferenciado, recuo, uma por vez), paginação intacta. `TransacaoDetalhes` virou componente compartilhado entre a linha expandida e o modal de estorno (DRY); colunas renomeadas (Valor bruto/Taxa) e tipo humanizado. 7 testes na transformação + tabela reescrita + verificação Playwright com pares reais criados via API |
| 2026-07-05 | "tem algo errado aqui - 2 linhas extornadas sem liquidacao... outra coisa eh que eu gostaria de buscar pelo tipo" — o usuário achou o limite real da heurística de pareamento da entrega anterior | Diagnosticar antes de corrigir: dump da API paginada mapeando cada linha à sua página | Causa confirmada com dados reais: os pares estavam em **páginas diferentes** (estornos na página 1, liquidações na 2) — a "degradação documentada" da versão anterior era ruim demais na prática. Correção na raiz em vez de remendo: o extrato passou a expor a referência exata (`liquidacaoEstornadaId` + data, LEFT JOIN na própria tabela), a heurística por recebível foi substituída por pareamento por id, e todo estorno virou expansível em qualquer página (original reconstruída da referência quando não veio na página). De quebra, filtro por tipo (Liquidação/Estorno) no backend + select na grid persistido na URL. Verificação Playwright do cenário exato reportado: os dois estornos órfãos agora expandem mostrando a liquidação de 04/07 |
| 2026-07-05 | "tem coisa estranha ainda - tem 2 transacoes que nao tem como estornar [linhas 'Estornada']" — terceira iteração do usuário sobre a mesma visão | Concluir a visão de estado final: linha "Estornada" é ruído quando a linha do Estorno já representa a operação | Exclusão no SQL do extrato (`not exists` no WHERE): liquidação já estornada não vem mais — paginação continua exata (nada de páginas encolhidas no cliente) e a operação segue 100% visível via linha do estorno expansível. A flag `estornada` ficaria permanentemente falsa, então saiu da cadeia inteira (SELECT → DTO → tipo TS → badge da UI) em vez de virar campo-mentira; o agrupamento do frontend encolheu de pareamento com consumo pra uma materialização simples da referência (−139 linhas no diff do frontend). Verificação varrendo todas as páginas da grid no navegador: zero linhas "Estornada", toda liquidação exibida estornável, todo estorno expansível |
| 2026-07-05 | "pq tem linhas da tabela que a taxa ta vazia" (linha BRL → USD com '—') | Preencher a taxa em operação cross-currency sem repetir o erro antigo de comparar moedas diferentes | O '—' era a proteção contra o bug do "82% sem sentido" (achado no Passo 15): a razão bruto/líquido em moedas diferentes não representa desconto. A solução correta: o extrato passou a expor o `valor_presente` (que sempre existiu na tabela, na moeda do título) e a taxa é calculada sobre ele — matematicamente válida pra qualquer par de moedas, coluna 100% preenchida. Curiosidade da verificação: o Playwright acusou "792.28%" que era artefato do próprio script (regex sobre o texto concatenado da linha, "180,79"+"2.28%"); a checagem por célula confirmou 2.28% e 5.83% corretos — mais um lembrete de validar a asserção antes de acusar o código |
| 2026-07-06 | Spec estruturada de UX em 7 seções (modal, timeline da origem, layout, badge, hierarquia, botões, animação) pedindo plano antes da implementação | Reforçar visualmente o vínculo estorno → operação de origem: a linha expandida parecia uma transação nova | Fluxo plan-mode completo (exploração → plano revisado → implementação): a linha de origem virou mini-timeline com cotovelo `└` desenhado por span absoluto com `border-l`+`border-b` (escala com a linha, ao contrário do caractere literal que desalinha por fonte), indentação de ~28px, badge âmbar único "Origem" (novo tom `warning` no Badge, usando o token `--color-warning` que estava declarado e sem uso) e texto/fundo rebaixados via `[&>td]:text-ink-muted` na `<tr>` (especificidade vence o `text-ink` base do `Td` sem tocar no componente). Animação com `motion` (fade+slide 200ms easeOut; timeline cresce com `scaleY` origin-top; sem animar height — `<tr>` não anima height de forma confiável), com `MotionGlobalConfig.skipAnimations` no setup de testes pro exit do `AnimatePresence` não adiar o unmount no jsdom. Verificação Playwright pegou um erro real do plano: `w-24` fixo quebrava "Ver origem" em 2 linhas (altura inconsistente) — corrigido com `whitespace-nowrap` |
| 2026-07-06 | Duas rodadas de feedback sobre a entrega acima ("a animação está errada, era pra deslizar de baixo da linha de estorno" → "tira a animação, ficou estranho, bota a seta ↳") + troca do favicon e revisão geral da documentação pré-release | Iterar o refinamento visual com o usuário no controle e alinhar todos os docs ao estado real antes do release | A animação evoluiu de fade+slide pra slide-de-trás-da-linha (translateY(-100%) com z-index/fundo opaco na linha do estorno) e acabou removida por decisão de produto — junto dela saiu a dependência `motion` inteira (instalada e desinstalada no mesmo dia; custo baixo porque o uso estava contido num componente). Lição: animação de tabela é cara de acertar e fácil de descartar — validar a versão estática primeiro teria economizado 2 ciclos. Favicon: logo SRM 256px (o browser reduz melhor do que o PNG 32px já borrado). Auditoria de docs achou 4 defasagens reais pós-PR#7: flag "estornada" citada no README/critérios (removida do sistema no Passo 16), filtro `tipo` ausente da tabela de endpoints, contagens de teste velhas (111→131 backend, 68→121 frontend) e ROADMAP sem o Passo 16 — tudo corrigido; verificação incluiu recontagem real (JUnit XML + vitest) em vez de confiar nos números escritos |
| 2026-07-06 | "faca um plano completo de como vamos implementar - Dashboard do Grafana com painéis reais" (plan-mode com 2 decisões de escopo levadas ao usuário: instrumentar métricas de negócio? validar no CI?) | Fechar o último nice-to-have com um dashboard que mostre domínio, não só JVM | Exploração achou 3 lacunas que o plano corrigiu de passagem: datasource sem uid fixo, histogramas de latência desligados (sem eles não existe p95) e zero métricas de negócio instrumentadas. Entrega em 3 camadas: `MetricasNegocio` (contadores `srm_*` por moeda, com o trade-off BigDecimal→double documentado no código), dashboard JSON provisionado (4 rows; estado do circuit breaker como `closed*1+half_open*2+open*3` com value mappings) e smoke test do CI validando Prometheus/Grafana/dashboard. A verificação com dados reais pegou um bug clássico de PromQL que o plano não previu: stats com `increase()` mostravam 0 porque a série do contador nasce já em 1 (sem delta na janela) — corrigido pra total acumulado. Valores conferidos ponta a ponta: Prometheus batia com a resposta da API (956.35 BRL / 855.30 USD) |
| 2026-07-06 | "crie uma pasta nova e crie scripts dentro para eu conseguir testar o dashboard... cenario1, 2, 3 etc" | Demonstrar o dashboard sob demanda: circuit breaker abrindo, rajada de erros, carga, tráfego de negócio e pico por rota | 5 cenários bash em `scripts/dashboard/` (com trap que restaura o provider mesmo em Ctrl-C, testado com `timeout -s INT`). A primeira execução real do cenário de erros achou **2 bugs de expectativa e 1 de código**: estorno de id inexistente responde 409 (não 404) e `page=-1` no extrato devolvia **500** — violação real do critério S4 que estava documentado como atendido. Fix no backend: `@Min/@Max` nos params de paginação + `HandlerMethodValidationException` no handler de 400; primeira tentativa usou `@Validated` na classe e o teste pegou o engano (Spring 6.1: `@Validated` troca a validação nativa de params por AOP que lança `ConstraintViolationException` não mapeada — sem a anotação, o MVC lança `HandlerMethodValidationException` nativamente). Scripts são também teste exploratório: exercitar a API do jeito que um usuário malicioso/descuidado faria encontrou o que os testes de unidade não cobriam |
| 2026-07-06 | "altere o readme para ser a apresentacao do projeto... pense como o recrutador... nao seja muito tecnico e tmb nao escreva demais" | README como porta de entrada auto-suficiente: o avaliador entende, roda e acha a evidência de cada critério sem ninguém explicar | Reescrita orientada a persona: pitch em linguagem de negócio, demonstração guiada de 5 min, tabela de entregas por nível do desafio, e um "mapa pra avaliação" ligando cada critério da seção 8 do enunciado ao documento/código que o evidencia. O conflito "não escreva demais" vs. conteúdo exigido (fundamentação teórica é critério 8.1) foi resolvido com `<details>` recolhíveis — a fundamentação completa e os guias de execução isolada continuam no README, sem virar parede de texto. Saíram o checklist de status (redundante com o ROADMAP), a tabela de 11 endpoints (Swagger é a fonte viva) e o passo-a-passo curl da resiliência (o cenario1.sh faz melhor). 190 → 157 linhas com todos os links validados por script |

> Novas entradas devem ser adicionadas à tabela conforme prompts relevantes forem usados (scaffolding, geração de massa de dados, refatoração de queries, geração de testes, etc.).

## Trechos onde a IA alucinou ou gerou código inseguro

Nenhuma alucinação de fato (código inventado que não existe), mas 8 erros reais cometidos ao longo do desenvolvimento, todos pegos por validação real (não por review de código) e corrigidos na mesma sessão em que apareceram:

1. **Tipo de coluna incompatível (`CHAR(3)` vs. `VARCHAR`)**: as migrations usavam `CHAR(3)` para código de moeda, mas as entidades JPA mapeavam como `String` genérico (que o Hibernate interpreta como `VARCHAR`). `ddl-auto: validate` recusou subir a aplicação — o erro só apareceu ao rodar de verdade contra o schema real, não ao só ler o código. Corrigido trocando as migrations pra `VARCHAR(3)` (mais idiomático em Postgres de qualquer forma).
2. **`criado_em` voltando `null` nas respostas da API**: colunas com `DEFAULT now()` no banco, mapeadas como `insertable = false` na entidade — o Hibernate nunca relê esse valor de volta depois do INSERT sem a anotação `@Generated`. Só foi percebido lendo a resposta JSON de verdade no smoke test manual, não teria aparecido em teste unitário isolado.
3. **CNPJ duplicado retornando 500 genérico em vez de 409**: a violação de `UNIQUE constraint` no banco não estava sendo traduzida pra uma exceção de domínio — só descoberto tentando cadastrar o mesmo cedente duas vezes manualmente.

Os itens 1–3 são o mesmo padrão: erros que só existem na fronteira entre código e banco de dados real, invisíveis numa leitura de código e só detectáveis rodando a aplicação de verdade — reforça por que a sessão insistiu em `docker compose` do zero + smoke test manual em vez de só confiar no build passar.

4. **Coluna "Deságio %" comparando valores em moedas diferentes** (frontend): a fórmula `(valorFace - valorLiquido) / valorFace` foi aplicada igual em toda linha do Grid de Transações, mas numa liquidação cross-currency (título em BRL, pago em USD) isso divide um valor em USD por um valor em BRL — o resultado (82%) parecia plausível o suficiente pra passar batido numa leitura de código, mas era matematicamente sem sentido. Só foi pego **olhando o screenshot** do Playwright durante a verificação manual, não pelo código nem pelos testes escritos antes da verificação — reforça que "testes passando" e "build verde" não substituem olhar a tela de verdade.
5. **Teste de `usePainelOperadorForm` assumindo que `resetField` funcionaria sem o campo estar registrado** (frontend): a primeira versão do teste chamava `form.setValue('valorFace', 1000)` direto (sem `register`) e esperava que `form.resetField('valorFace')` voltasse o campo pro default depois da submissão — passou a asserção de que a submissão funcionava, mas falhou a de que o campo resetava (`1000` continuava lá). Causa raiz: no react-hook-form, `resetField` é efetivamente um no-op em campos que nunca passaram por `register()`/`Controller` — em produção isso não é um bug (o `<input>` real do `RecebivelForm` registra o campo), mas no teste do hook isolado (sem montar o formulário de verdade) faltava simular esse registro. Investigado com um teste isolado descartável (`probe.test.tsx`) até confirmar a causa via `register()` explícito antes do `setValue`, documentado com um comentário no teste final — não foi um bug no código de produção, foi uma lacuna na simulação do ambiente de teste.

6. **Query param obrigatório ausente retornando 500 em vez de 400**: `GlobalExceptionHandler` tinha um `@ExceptionHandler(Exception.class)` catch-all que intercepta `MissingServletRequestParameterException`/`MethodArgumentTypeMismatchException` do próprio Spring MVC antes da resolução padrão do framework — `GET /api/taxas-cambio` sem `moedaOrigem`, por exemplo, virava um `ERRO_INESPERADO` 500 em vez de um 400 claro. Só foi descoberto escrevendo o `@WebMvcTest` de `CambioController` e reparando que o status não batia com o esperado; corrigido com um `@ExceptionHandler` dedicado antes do catch-all, coberto por teste em cada controller GET afetado.
7. **`FiltrosTransacoes` sem `name`/`id` nos campos** (frontend): os `<Select>`/`<DateField>` do filtro de transações não recebiam `name` nem `id`, então o `<label htmlFor={...}>` interno desses componentes nunca tinha um valor pra apontar — o rótulo visualmente aparecia ao lado do campo, mas não estava associado a ele via `for`/`id` (falha real de acessibilidade: leitor de tela não liga o rótulo ao controle). Só apareceu ao tentar `getByLabelText` no teste de `FiltrosTransacoes`; corrigido adicionando `name` a cada campo.

Os itens 6 e 7 reforçam o mesmo padrão dos itens 1–5: bugs que uma leitura de código não pega, só aparecem exercitando o comportamento de verdade — nesses dois casos, especificamente, foi o próprio ato de *escrever o teste* (não rodar a aplicação) que expôs o problema, o que também é uma resposta direta ao critério de avaliação "a IA potencializou a engenharia ou mascarou falta de conhecimento?": aqui a cobertura de teste foi tratada como ferramenta de descoberta de bug, não só como checklist a cumprir.

8. **`ExtratoLiquidacaoRepositoryIT` passando localmente (leitura do código) mas quebrando 3x seguidas no CI real**: primeiro erro — múltiplos `@Test` compartilhando um `@BeforeEach` contra o mesmo container Testcontainers (não recriado por método) sem isolamento, batendo na UNIQUE constraint de `documento` do cedente semeado no teste anterior; corrigido com `@Transactional` na classe (rollback automático por método). Segundo erro, mais sutil: com tudo dentro de uma única transação de teste que nunca commita, o INSERT da última `Liquidacao` criada no `seed()` ficava pendente no persistence context do Hibernate — as leituras seguintes usam `JdbcTemplate`/`NamedParameterJdbcTemplate` (SQL puro, fora do `EntityManager`), que nunca disparam o auto-flush do Hibernate, então a liquidação mais recente simplesmente não aparecia pra query. Só ficou visível depois de adicionar `testLogging { exceptionFormat = FULL }` no Gradle pra ver a mensagem real da asserção (`expected: 3L but was: 2L`) nos logs do GitHub Actions — sem isso, o console só mostrava o tipo da exceção, não o diff. Corrigido com um `entityManager.flush()` explícito antes das leituras. Como este ambiente de desenvolvimento local não roda Testcontainers, todo esse ciclo de diagnóstico foi feito via push + `gh run watch` no CI real, iterativamente — reforça, de novo, que rodar a aplicação/teste de verdade (aqui, via CI) encontra bugs que nenhuma leitura de código pega.

## Análise crítica

**Onde a IA economizou tempo:**
- Scaffolding completo de backend (Gradle/Spring Boot) e frontend (Vite/React/TS), incluindo escolha e resolução de versões atuais de todas as dependências via consulta direta ao Maven Central/npm registry — trabalho mecânico que seria repetitivo fazer manualmente.
- Configuração de Husky/lint-staged/commitlint (3 hooks) escrita, testada e commitada de ponta a ponta numa única sessão, incluindo a validação real de cada hook (não só "deveria funcionar").
- Revisão do rascunho de modelo de dados por um agente dedicado encontrou lacunas reais de precisão numérica (`NUMERIC` sem escala) e de modelagem (`status` binário incompleto) antes de qualquer linha de DDL ser escrita — mais barato corrigir no papel do que depois de entidades JPA já existirem em cima.

**Onde a IA exigiu filtro humano (não foi aceito por padrão):**
- O agente de planejamento do modelo de dados sugeriu 3 extensões de escopo "de graça" (tabela `movimento_caixa`, coluna `liquidacao.criado_por`, ponteiro redundante `recebivel.ultima_liquidacao_id`) que, se aceitas sem questionar, teriam inflado o modelo com complexidade não pedida pelo desafio — duas foram adiadas explicitamente como gap documentado, uma foi decidida via pergunta objetiva ao usuário em vez de assumida.
- Ao gerar o `.gitignore` inicial, esqueci de cobrir `node_modules/` na raiz do monorepo (só cobri `frontend/node_modules/`) — isso quase resultou em commitar ~100 pacotes de `node_modules` do Husky/commitlint no histórico do Git. Percebido antes do commit (`git status --short` mostrando centenas de arquivos inesperados) e corrigido no `.gitignore` antes de qualquer commit sujar o repositório.

**Onde a IA atrapalhou (tempo perdido em rabbit hole):**
- O teste de integração de concorrência (Testcontainers) falhou neste ambiente de desenvolvimento porque o Docker Engine local é mais novo do que o probe de compatibilidade fixo no Testcontainers 1.21.x. Foi investigado a fundo (inspeção de bytecode do jar, teste direto no socket do Docker via `curl`, tentativa de forçar versão mais nova de `docker-java` via Gradle) até confirmar que é uma limitação real do ambiente, não do código — decisão final foi não mascarar isso (ex.: trocar silenciosamente pra uma abordagem que "funciona aqui" sem avisar), e sim documentar a causa raiz, manter o teste como está (correto para ambientes normais) e validar o mesmo cenário manualmente contra a API real.

**Decisões deliberadamente humanas, não apenas aceitas de sugestão da IA:**
- Toda decisão de domínio (Passo 1 do `ROADMAP.md`) foi conduzida por perguntas do usuário pedindo explicação simples antes de decidir — a IA nunca decidiu regra de negócio sozinha, só explicou trade-offs para decisão humana.
- Escolha de stack (Gradle vs. Maven, monorepo vs. multi-repo, PostgreSQL vs. MySQL) e escopo do modelo de dados (spread no código vs. no banco, persistir lote ou não) foram sempre confirmados via pergunta explícita ao usuário antes de implementar, nunca assumidos silenciosamente.
