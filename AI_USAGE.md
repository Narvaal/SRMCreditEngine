# AI_USAGE.md

Registro do uso de IA (Claude Code) no desenvolvimento do SRM Credit Engine, conforme a política de uso de IA do desafio (ver `CLAUDE.md`, seção 2). Este documento é atualizado incrementalmente ao longo do desenvolvimento, não apenas ao final.

## Ferramenta utilizada

- **Claude Code** (Anthropic), modelo Sonnet 5 — sessão via CLI.

## Prompts estratégicos utilizados

| Data | Prompt (resumo) | Objetivo | Resultado |
|------|------------------|----------|-----------|
| 2026-07-03 | "escreva um claude md com essas informações [enunciado completo do desafio]" | Registrar o enunciado do desafio como contexto persistente do projeto, para orientar sessões futuras de desenvolvimento assistido por IA | `CLAUDE.md` criado com o enunciado completo estruturado por seção, mais notas práticas de orientação (stack, precisão decimal, workflow de Git por senioridade) |
| 2026-07-03 | "crie tmb o AI_USAGE.md" | Criar o arquivo de rastreabilidade de uso de IA exigido pelo desafio | Este arquivo |
| 2026-07-03 | Sequência de perguntas pedindo pra explicar o problema de negócio "da forma mais simples possível" (fluxo de deságio, ordem da conversão de moeda, o que é um lote de recebíveis) | Construir entendimento do domínio antes de tomar qualquer decisão técnica (metodologia SDD) | Explicações em linguagem simples com exemplos numéricos concretos, sem gerar código — usadas como base para as decisões registradas em seguida |
| 2026-07-03 | Fechamento guiado das perguntas em aberto do `ROADMAP.md` (granularidade da liquidação por lote vs. por item; origem da Taxa Base — mercado vs. interna; convenção de prazo em dias vs. meses; existência de estorno/cancelamento; controle de saldo de caixa por moeda) | Eliminar as ambiguidades de negócio do enunciado antes de especificar modelo de dados e API | 5 decisões de domínio registradas no `ROADMAP.md`, cada uma com a justificativa de negócio por trás: (1) liquidação independente por recebível, não por lote; (2) Taxa Base é taxa de mercado (CDI/SOFR) com histórico, spread é política interna da SRM; (3) prazo em meses, mês fixo = 30 dias, convenção simplificada e assumida conscientemente; (4) histórico append-only, correções via estorno, nunca reescrita do passado; (5) caixa com saldo controlado por moeda, habilitando o cenário real de Optimistic Locking do nível Sênior |
| 2026-07-03 | "faça um plano e me apresente" (modelo de dados) — rascunho inicial de 8 entidades revisado por um agente de planejamento dedicado, com instrução explícita para criticar tipos numéricos, índices, constraints e apontar entidades/relacionamentos faltantes sob a ótica de um sistema financeiro real | Validar o esquema de dados antes de implementar as migrations, evitando erros de precisão numérica ou modelagem que só apareceriam depois de código de aplicação já escrito em cima | O agente encontrou problemas reais no rascunho: `NUMERIC` sem precisão/escala definida (risco real de inconsistência num sistema financeiro), `recebivel.status` binário incompleto (não suportava o fluxo de estorno já decidido), e CHECK constraints de integridade ausentes (ex.: invariante moeda-título × taxa-de-câmbio). Também sugeriu 3 extensões de escopo (tabela `lote_importacao`, ponteiro redundante em `recebivel`, spread no banco vs. no código) — cada uma foi levada de volta pro usuário decidir via pergunta objetiva, não aceita automaticamente. Onde a IA ajudou: achou a lacuna de `NUMERIC` genérico e o `status` binário incompleto, que eu não tinha notado no rascunho. Onde exigiu filtro humano: sugeriu adicionar `movimento_caixa` e `liquidacao.criado_por` "de graça" — decidi adiar as duas por escopo (a segunda nem faz sentido ainda, não existe sistema de usuário) em vez de aceitar por padrão só porque a IA recomendou. |

| 2026-07-03 | "o docker compose já tá pronto, se não tiver faça ele subir o app e banco de dados" | Orquestrar API + PostgreSQL + Prometheus + Grafana num único `docker compose up`, item já pendente no checklist do README | `docker-compose.yml` + `backend/Dockerfile` (build multi-stage) + `infra/prometheus/prometheus.yml` + provisionamento automático do datasource do Grafana. Validado de verdade: build da imagem, subida dos 4 containers, migrations aplicando no boot, `/actuator/health` respondendo, Prometheus com o target `up`, Grafana com o datasource já configurado — e um ciclo completo `down -v` + `up -d` do zero pra garantir que não era só cache local funcionando por acaso |
| 2026-07-03 | Discussão de arquitetura (Controller/Service/DAO, depois refinada pra Controller/Service/Repository + caminho de 2 camadas pro relatório) seguida de "faça um plano e me apresente" — plano revisado por um agente dedicado com perguntas técnicas específicas (potência fracionária em `BigDecimal`, geração de UUID em JPA, mecânica de Optimistic Locking, assinatura do Strategy) | Implementar a camada de aplicação inteira (entidades JPA, motor de precificação, services transacionais, controllers, relatório) em cima do modelo de dados já validado | Aplicação funcional de ponta a ponta: 9 entidades, motor de precificação com `big-math` pra expoente fracionário, `LiquidacaoService` com Optimistic Locking real (flush explícito + tradução de exceção), 15 testes unitários de precificação, endpoints REST completos, relatório em 2 camadas com SQL nativo. Validado com `docker compose` do zero + smoke test manual completo + teste de concorrência real via 2 requisições HTTP simultâneas |
| 2026-07-03 | "faça um plano para implementar o front, gostaria algo minimalista e moderno, com as cores verde e preto" — plano revisado por um agente dedicado, que primeiro apontou que o requisito de "cálculo em tempo real" não era implementável sem um endpoint novo de simulação (duplicar a fórmula no frontend duplicaria regra de negócio) | Implementar o Painel do Operador e o Grid de Transações com a arquitetura de separação apresentação/negócio exigida pelo desafio | 3 endpoints novos no backend (`/simular`, `/moedas`, `/tipos-recebivel`) + frontend completo (Tailwind v4 tema escuro/verde, React Router, TanStack Query, React Hook Form + Zod), validado com Playwright headless (simulação em tempo real, submissão de lote, navegação, zero erros de console) |
| 2026-07-03 | "faça o workflow de ci/cd usando o github actions e adicione o frontend ao docker-compose" | Fechar o requisito de CI/CD do nível Sênior e eliminar a lacuna do frontend não containerizado | `frontend/Dockerfile` (multi-stage, Nginx com reverse proxy de `/api` pro backend) + `docker-compose.yml` com o serviço `frontend` + `.github/workflows/ci.yml` (3 jobs: backend, frontend, smoke test do compose completo). Validado com `docker compose up -d --build` real (5 containers, proxy funcionando, fallback de SPA) e o YAML do workflow checado com `actionlint` (baixado no scratchpad, já que `act` não estava disponível pra rodar Actions localmente) |
| 2026-07-04 | "leia o roadmap e readme... quero que vc me apresente um relatorio do que devemos testar" seguido de "vamos fazer na ordem" (execução item a item) | Fechar a lacuna de cobertura de testes identificada no próprio `ROADMAP.md` (`LiquidacaoService` só testado indiretamente; vários services sem teste próprio) | Relatório priorizado (P0/P1/P2) mapeando cada gap contra o código real, seguido de implementação incremental: `LiquidacaoServiceTest` (14 casos: liquidar/estornar, saldo insuficiente, Optimistic Locking, estorno duplicado), `LiquidacaoBatchServiceTest` (resultado parcial do lote), `CambioServiceTest`, `TaxaMercadoServiceTest`, `RecebivelServiceTest`, `CedenteServiceTest`, `GlobalExceptionHandlerTest` (68 testes de unidade no total, backend 100% verde exceto a limitação conhecida do Testcontainers), e no frontend `usePainelOperadorForm.test.tsx` + `useExtratoFiltrosUrlState.test.tsx` |
| 2026-07-04 | "faca os comits atomicos, e faca rebase para contar a historia de melhor forma" | Praticar o requisito de Git explícito do nível Sênior (organizar/reordenar commits via `git rebase -i` antes de um merge), até então só planejado e nunca demonstrado | 7 commits atômicos (`test(backend): ...`, um por classe testada) criados fora da ordem narrativa ideal de propósito, depois reordenados via `git rebase -i` (com `GIT_SEQUENCE_EDITOR` roteirizado, sem editor interativo) pra sequência núcleo-transacional → services de domínio → cross-cutting handler — suíte revalidada verde após o rebase antes do push |
| 2026-07-04 | "vamos atacar o problema dos logs estruturados, faca um plano para implementar, entenda qual a melhor opcao para o projeto ecs ou gelf e me apresente" | Fechar outro requisito explícito do nível Sênior (observabilidade via logs estruturados), decidindo entre os dois formatos nativos do Spring Boot 3.5 | Levantamento do estado real (zero config de logging, só 2 linhas de log em todo o backend) fundamentou a recomendação por **ECS** em vez de GELF (GELF pressupõe um Graylog que o projeto não roda; ECS é o formato nativo tanto de um eventual Elastic quanto do Grafana Loki, extensão natural do Grafana já provisionado). Plano aprovado via `EnterPlanMode`/`ExitPlanMode` antes de codar. Implementado: `logging.structured.format.console: ecs`, `CorrelationIdFilter` (MDC `requestId` por requisição) e logs de negócio estruturados (`addKeyValue`) em `LiquidacaoService`/`LiquidacaoBatchService`/`GlobalExceptionHandler` |
| 2026-07-04 | "quero que voce cubra os testes que ainda estao em aberto" (backend P2 + frontend P1 do relatório de cobertura) | Fechar os últimos gaps de teste antes do merge/release: controllers, relatório (SQL nativo) e componentes de composição do frontend | `@WebMvcTest` nos 8 controllers, `ExtratoLiquidacaoRepositoryIT` (Testcontainers), e no frontend `useExtratoLiquidacaoQuery`/`RecebivelForm`/`FiltrosTransacoes`/`PainelOperadorPage`/`GridTransacoesPage`. Escrever os testes achou 2 bugs reais de verdade (detalhados na seção abaixo), corrigidos na hora: `@RequestParam` obrigatório ausente devolvendo 500 em vez de 400, e campos de filtro sem `name`/`id` quebrando a associação `label`↔`input` (acessibilidade) |
| 2026-07-04 | "escreva um documento com os Critérios de aceite documentados... e tambem o diagrama c4, nivel 1 e 2" | Fechar os dois últimos requisitos de documentação explícitos do desafio (seções 5 e 6) antes do merge/release | `docs/diagrama-c4.md` (Mermaid `C4Context`/`C4Container`, mesma convenção do diagrama ER) e `docs/criterios-aceite.md` (tabela verificável por dimensão — usabilidade/segurança/desempenho/escalabilidade — com status e evidência por item, não prosa vaga). Levantamento do código real (ausência de Spring Security, Dockerfiles sem `USER`, `docker-compose` em HTTP puro, sem cache/teste de carga) tornou explícitos gaps que já existiam implicitamente mas nunca tinham sido escritos num lugar só |
| 2026-07-04 | "ele ta meio estranho visualmente... tem alguma forma de alterar isso" (diagrama C4) | Corrigir texto sobreposto e caixas pequenas nos diagramas C4 renderizados | Diagnosticado como limitação do motor de layout do tipo `C4Context`/`C4Container` nativo do Mermaid (caixas de tamanho fixo). Reescrito como `flowchart` estilizado como C4 (classDefs pra `person`/`system`/`container`/`external`), com o motor de layout padrão do Mermaid (dagre), que ajusta caixa ao conteúdo. Validado renderizando de verdade pra PNG via `mermaid-cli` (não só lendo o código-fonte) antes de cada commit, inclusive iterando sobre uma aresta que visualmente parecia sair do PostgreSQL em vez do backend (artefato de layout, corrigido com estilo tracejado + nota explicativa) |
| 2026-07-04 | "vamos fazer o merge dev → main" seguido de "cria a tag v1.0.0" e "faz os dois: atualiza o roadmap e cria a release" | Primeira promoção do projeto completo pra `main` e marco de versão semântica, fechando os últimos critérios de Git do nível Sênior (`CLAUDE.md`, seção 6) | PR #1 (`dev → main`, 71 commits, CI validado no PR antes do merge) com descrição resumindo os 10 passos do roadmap; merge commit (não squash, preserva a granularidade dos commits atômicos); tag anotada `v1.0.0` sobre o commit de merge; GitHub Release publicada a partir da tag. Confirmado com `gh run watch` que o CI também passa direto em `main` depois do merge, não só no PR |

> Novas entradas devem ser adicionadas à tabela conforme prompts relevantes forem usados (scaffolding, geração de massa de dados, refatoração de queries, geração de testes, etc.).

## Trechos onde a IA alucinou ou gerou código inseguro

Nenhuma alucinação de fato (código inventado que não existe), mas 3 erros reais cometidos na primeira tentativa, todos pegos por validação real (não por review de código) e corrigidos na mesma sessão:

1. **Tipo de coluna incompatível (`CHAR(3)` vs. `VARCHAR`)**: as migrations usavam `CHAR(3)` para código de moeda, mas as entidades JPA mapeavam como `String` genérico (que o Hibernate interpreta como `VARCHAR`). `ddl-auto: validate` recusou subir a aplicação — o erro só apareceu ao rodar de verdade contra o schema real, não ao só ler o código. Corrigido trocando as migrations pra `VARCHAR(3)` (mais idiomático em Postgres de qualquer forma).
2. **`criado_em` voltando `null` nas respostas da API**: colunas com `DEFAULT now()` no banco, mapeadas como `insertable = false` na entidade — o Hibernate nunca relê esse valor de volta depois do INSERT sem a anotação `@Generated`. Só foi percebido lendo a resposta JSON de verdade no smoke test manual, não teria aparecido em teste unitário isolado.
3. **CNPJ duplicado retornando 500 genérico em vez de 409**: a violação de `UNIQUE constraint` no banco não estava sendo traduzida pra uma exceção de domínio — só descoberto tentando cadastrar o mesmo cedente duas vezes manualmente.

Todos os 3 são o mesmo padrão: erros que só existem na fronteira entre código e banco de dados real, invisíveis numa leitura de código e só detectáveis rodando a aplicação de verdade — reforça por que a sessão insistiu em `docker compose` do zero + smoke test manual em vez de só confiar no build passar.

6. **Query param obrigatório ausente retornando 500 em vez de 400**: `GlobalExceptionHandler` tinha um `@ExceptionHandler(Exception.class)` catch-all que intercepta `MissingServletRequestParameterException`/`MethodArgumentTypeMismatchException` do próprio Spring MVC antes da resolução padrão do framework — `GET /api/taxas-cambio` sem `moedaOrigem`, por exemplo, virava um `ERRO_INESPERADO` 500 em vez de um 400 claro. Só foi descoberto escrevendo o `@WebMvcTest` de `CambioController` e reparando que o status não batia com o esperado; corrigido com um `@ExceptionHandler` dedicado antes do catch-all, coberto por teste em cada controller GET afetado.
7. **`FiltrosTransacoes` sem `name`/`id` nos campos** (frontend): os `<Select>`/`<DateField>` do filtro de transações não recebiam `name` nem `id`, então o `<label htmlFor={...}>` interno desses componentes nunca tinha um valor pra apontar — o rótulo visualmente aparecia ao lado do campo, mas não estava associado a ele via `for`/`id` (falha real de acessibilidade: leitor de tela não liga o rótulo ao controle). Só apareceu ao tentar `getByLabelText` no teste de `FiltrosTransacoes`; corrigido adicionando `name` a cada campo.

Os itens 6 e 7 reforçam o mesmo padrão dos itens 1–5: bugs que uma leitura de código não pega, só aparecem exercitando o comportamento de verdade — nesses dois casos, especificamente, foi o próprio ato de *escrever o teste* (não rodar a aplicação) que expôs o problema, o que também é uma resposta direta ao critério de avaliação "a IA potencializou a engenharia ou mascarou falta de conhecimento?": aqui a cobertura de teste foi tratada como ferramenta de descoberta de bug, não só como checklist a cumprir.

8. **`ExtratoLiquidacaoRepositoryIT` passando localmente (leitura do código) mas quebrando 3x seguidas no CI real**: primeiro erro — múltiplos `@Test` compartilhando um `@BeforeEach` contra o mesmo container Testcontainers (não recriado por método) sem isolamento, batendo na UNIQUE constraint de `documento` do cedente semeado no teste anterior; corrigido com `@Transactional` na classe (rollback automático por método). Segundo erro, mais sutil: com tudo dentro de uma única transação de teste que nunca commita, o INSERT da última `Liquidacao` criada no `seed()` ficava pendente no persistence context do Hibernate — as leituras seguintes usam `JdbcTemplate`/`NamedParameterJdbcTemplate` (SQL puro, fora do `EntityManager`), que nunca disparam o auto-flush do Hibernate, então a liquidação mais recente simplesmente não aparecia pra query. Só ficou visível depois de adicionar `testLogging { exceptionFormat = FULL }` no Gradle pra ver a mensagem real da asserção (`expected: 3L but was: 2L`) nos logs do GitHub Actions — sem isso, o console só mostrava o tipo da exceção, não o diff. Corrigido com um `entityManager.flush()` explícito antes das leituras. Como este ambiente de desenvolvimento local não roda Testcontainers, todo esse ciclo de diagnóstico foi feito via push + `gh run watch` no CI real, iterativamente — reforça, de novo, que rodar a aplicação/teste de verdade (aqui, via CI) encontra bugs que nenhuma leitura de código pega.

4. **Coluna "Deságio %" comparando valores em moedas diferentes** (frontend): a fórmula `(valorFace - valorLiquido) / valorFace` foi aplicada igual em toda linha do Grid de Transações, mas numa liquidação cross-currency (título em BRL, pago em USD) isso divide um valor em USD por um valor em BRL — o resultado (82%) parecia plausível o suficiente pra passar batido numa leitura de código, mas era matematicamente sem sentido. Só foi pego **olhando o screenshot** do Playwright durante a verificação manual, não pelo código nem pelos testes escritos antes da verificação — reforça que "testes passando" e "build verde" não substituem olhar a tela de verdade.
5. **Teste de `usePainelOperadorForm` assumindo que `resetField` funcionaria sem o campo estar registrado** (frontend): a primeira versão do teste chamava `form.setValue('valorFace', 1000)` direto (sem `register`) e esperava que `form.resetField('valorFace')` voltasse o campo pro default depois da submissão — passou a asserção de que a submissão funcionava, mas falhou a de que o campo resetava (`1000` continuava lá). Causa raiz: no react-hook-form, `resetField` é efetivamente um no-op em campos que nunca passaram por `register()`/`Controller` — em produção isso não é um bug (o `<input>` real do `RecebivelForm` registra o campo), mas no teste do hook isolado (sem montar o formulário de verdade) faltava simular esse registro. Investigado com um teste isolado descartável (`probe.test.tsx`) até confirmar a causa via `register()` explícito antes do `setValue`, documentado com um comentário no teste final — não foi um bug no código de produção, foi uma lacuna na simulação do ambiente de teste.

## Análise crítica

**Onde a IA economizou tempo:**
- Scaffolding completo de backend (Gradle/Spring Boot) e frontend (Vite/React/TS), incluindo escolha e resolução de versões atuais de todas as dependências via consulta direta ao Maven Central/npm registry — trabalho mecânico que seria repetitivo fazer manualmente.
- Configuração de Husky/lint-staged/commitlint (3 hooks) escrita, testada e commitada de ponta a ponta numa única sessão, incluindo a validação real de cada hook (não só "deveria funcionar").
- Revisão do rascunho de modelo de dados por um agente dedicado encontrou lacunas reais de precisão numérica (`NUMERIC` sem escala) e de modelagem (`status` binário incompleto) antes de qualquer linha de DDL ser escrita — mais barato corrigir no papel do que depois de entidades JPA já existirem em cima.

**Onde a IA exigiu filtro humano (não foi aceito por padrão):**
- O agente de planejamento do modelo de dados sugeriu 3 extensões de escopo "de graça" (tabela `movimento_caixa`, coluna `liquidacao.criado_por`, ponteiro redundante `recebivel.ultima_liquidacao_id`) que, se aceitas sem questionar, teriam inflado o modelo com complexidade não pedida pelo desafio — duas foram adiadas explicitamente como gap documentado, uma foi decidida via pergunta objetiva ao usuário em vez de assumida.
- Ao gerar o `.gitignore` inicial, esqueci de cobrir `node_modules/` na raiz do monorepo (só cobri `frontend/node_modules/`) — isso quase resultou em commitar ~100 pacotes de `node_modules` do Husky/commitlint no histórico do Git. Percebido antes do commit (`git status --short` mostrando centenas de arquivos inesperados) e corrigido no `.gitignore` antes de qualquer commit sujar o repositório.

**Onde a IA atrapalhou (tempo perdido em rabbit hole):**
- O teste de integração de concorrência (Testcontainers) falhou neste ambiente de desenvolvimento porque o Docker Engine local é mais novo do que o probe de compatibilidade fixo no Testcontainers 1.21.x. Foi investigado a fundo (inspeção de bytecode do jar, teste direto no socket do Docker via `curl`, tentativa de forçar versão mais nova de `docker-java` via Gradle) até confirmar que é uma limitação real do ambiente, não do código — decisão final foi não mascarar isso (ex.: trocar silenciosamente pra uma abordagem que "funciona aqui" sem avisar), e sim documentar a causa raiz, manter o teste como está (correto para ambientes normais) e validar o mesmo cenário manualmente contra a API real.

**Decisões deliberadamente humanas, não apenas aceitas de sugestão da IA:**
- Toda decisão de domínio (Passo 1 do `ROADMAP.md`) foi conduzida por perguntas do usuário pedindo explicação simples antes de decidir — a IA nunca decidiu regra de negócio sozinha, só explicou trade-offs para decisão humana.
- Escolha de stack (Gradle vs. Maven, monorepo vs. multi-repo, PostgreSQL vs. MySQL) e escopo do modelo de dados (spread no código vs. no banco, persistir lote ou não) foram sempre confirmados via pergunta explícita ao usuário antes de implementar, nunca assumidos silenciosamente.
