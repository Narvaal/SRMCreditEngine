# Migrations (Flyway)

Scripts DDL versionados do banco de dados vivem aqui, seguindo a convenção do Flyway: `V<versão>__<descrição>.sql` (ex.: `V1__create_moeda.sql`).

Ainda vazio — as migrations serão adicionadas junto com o modelo de dados (Diagrama ER + DDL), próximo passo do roadmap.
