# Dashboards do Grafana

Ainda vazio. Dashboards em JSON exportado do Grafana entram aqui quando existirem métricas de negócio reais pra visualizar (ex.: volume de liquidações, latência de precificação). Por ora, só o datasource do Prometheus está provisionado automaticamente (`../datasources/prometheus.yml`).
