apiVersion: 1

providers:
  - name: srm
    folder: SRM Credit Engine
    type: file
    # Dashboard é código: edições pela UI não persistem — exportar o JSON e commitar.
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
